# vaultwarden.example.com

server {
    listen 443 ssl;
    http2 on;

    server_name vaultwarden.example.com;

    ssl_certificate     /etc/letsencrypt/live/vaultwarden.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/vaultwarden.example.com/privkey.pem;

    error_log  /var/log/openresty/com.example.vaultwarden_nginx_err.log warn;
    access_log /var/log/openresty/com.example.vaultwarden_nginx_acc.log combined;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        access_by_lua_block {
            local core = require "waf.core"
            local app  = require "waf.apps.vaultwarden"
            core.run(app)
        }
    }
}
